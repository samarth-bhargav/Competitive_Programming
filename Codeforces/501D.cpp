#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll n,k,s;
    cin >> n >> k >> s;
    if (k > s || k*(n-1) < s){
        cout << "NO\n";
    }
    else{
        
    }
}

